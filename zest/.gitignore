# Xcode / build artifacts
xcuserdata/
DerivedData/
build/
.DS_Store

# We DO commit Zest.xcodeproj so you can open it without any tools.
# (If you regenerate it with XcodeGen from project.yml, that's fine too —
#  it will just overwrite the committed one with the full multi-target setup.)
